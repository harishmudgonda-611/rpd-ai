import test from'node:test';import assert from'node:assert/strict';import{mkdtemp,writeFile}from'node:fs/promises';import{tmpdir}from'node:os';import{join}from'node:path';import{scanModelAssets,selectBestModelAsset}from'./model-assets.js';import{TryOnEngine}from'./tryon.js';
test('scans RPD model assets',async()=>{const root=await mkdtemp(join(tmpdir(),'rpd-models-'));const png=Buffer.alloc(24);png.write('\x89PNG\r\n\x1a\n',0,'binary');png.writeUInt32BE(1080,16);png.writeUInt32BE(1920,20);await writeFile(join(root,'front_kurti_model.png'),png);const assets=await scanModelAssets(root);assert.equal(assets.length,1);assert.equal(assets[0].pose,'front');assert.ok(assets[0].categoryCompatibility.includes('upper_body'));assert.equal(assets[0].width,1080);assert.equal(selectBestModelAsset(assets,'upper_body','front')?.assetId,assets[0].assetId)});
test('does not fake try-on',async()=>{const result=await new TryOnEngine([]).generate({personImage:'model.png',garmentImage:'dress.png',category:'dress',outputPath:'out.png'});assert.equal(result.status,'unsupported');assert.equal(result.provider,'none')});
