import test from'node:test';import assert from'node:assert/strict';import{analyzeVariants,normalizeColorName}from'./analyzer.js';test('normalizes colour aliases',()=>assert.equal(normalizeColorName('Wine'),'maroon'));test('deduplicates colours',()=>{const r=analyzeVariants([{color:'Wine',size:'M',sku:'A1'},{color:'Maroon',size:'M',sku:'A2'},{color:'Pink',size:'M',sku:'A3'}]);assert.equal(r.uniqueColorCount,2)});test('creates explicit colour variants',()=>assert.equal(analyzeVariants([],['Black','Pink','Green']).variants.length,3));
