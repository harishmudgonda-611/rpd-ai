# Testing Module 03

From repository root:

```bash
npm install
npx tsx --test modules/variant-colour-intelligence/analyzer.test.ts
```

Expected: 4 passing tests.

The module is deterministic and does not require a paid API, network call, or external model for its core tests.
